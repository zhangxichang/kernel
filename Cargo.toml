[package]
name = "kernel"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "kernel"
test = false
bench = false

[dependencies]
glam = { version = "0.32.1", default-features = false, features = ["libm"] }
